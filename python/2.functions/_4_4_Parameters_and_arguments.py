def multiplyy(x, y): # x & y are formal parameters (not formal arguments)  
result = x * y  
return result  
  
zxe = multiplyy(3, 7) # passing 3 & 7 as arguments  
print(zxe)  
  
xcn = multiplyy(7.86, 5)  
print(xcn)  
print()  
  
for i in range(1, 5):  
	ftr = multiplyy(i, 7)  
	print(ftr)  
  
# ### Parameters  
# parameters are like placeholders, they're variables &  
# the variables will be given values(arguments) each time we pass it into function  
# here x, y are formal parameters and they should not be called formal arguments  
  
# ### Arguments  
# we pass arguments/values into the function through the the formal parameters
